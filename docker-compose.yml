version: '3'

services:
  web:
    build: .
    volumes:
      - .:/app
      - gem_cache:/gems
    environment:
      - ALMA_API_HOST=https://api-na.hosted.exlibrisgroup.com
    env_file:
      - .env
volumes:
  gem_cache:
